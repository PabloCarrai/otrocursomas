computador = {
    "id": 1001,
    "marca": "MSi",
    "ram": 128,
    "cpu": "i386",
    "almacenamiento": 8,
}
#   Metodo clear()
print(computador)
print(f"Cantidad de llaves en computador {len(computador)}")
computador.clear()
print(computador)
print(f"Cantidad de llaves en computador {len(computador)}")
